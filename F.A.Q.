* On which operating systems can the DeepCleanup.ps1 script be run?
* Can the DeepCleanup.ps1 script be used on Windows 10?
* Is Windows 11 compatible with the DeepCleanup.ps1 script?
* Can the DeepCleanup.ps1 script be executed on Windows Server editions?
* What is required to execute the DeepCleanup.ps1 script on these systems?

The DeepCleanup.ps1 script is designed to run on Windows operating systems. Specifically, it can be used on:

- Windows 10
- Windows 11
- Windows Server editions

These systems support PowerShell, which is required to execute the scripts (1 and 2).
